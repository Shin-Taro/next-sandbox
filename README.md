## This is a demonstration of navigating across Next.js and an external server.

## Usage

### Install dependencies
`yarn`

### Launch all services
`docker compose up -d`

### Launch Next.js dev server
`yarn dev`

### Launch external we server
`docker compose up external -d`
