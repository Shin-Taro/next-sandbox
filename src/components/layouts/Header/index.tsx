import { FC } from "react"

export const Header: FC = () => (
  <header className="border-2 border-sub py-5 text-center text-first">
    <h1>Demo for navigation between Next.js app and other server</h1>
  </header>
)
